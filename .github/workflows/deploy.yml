name: Deploy to VPS

on:
  push:
    branches: [ main ]

jobs:
  changelog:
    name: "🚀 Semantic Release (Version & Changelog)"
    runs-on: ubuntu-latest
    permissions:
      contents: write
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && !contains(github.event.head_commit.message, '[skip ci]')
    steps:
      - name: "📦 Checkout code"
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Важно: semantic-release нужна полная история коммитов
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: "🐍 Setup Python"
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: "📋 Install semantic-release"
        run: pip install semantic-release

      - name: "🚀 Run Semantic Release (Dry-run first)"
        run: |
          # Сначала проверяем, будет ли релиз
          if semantic-release version --dry-run; then
            echo "🔴 New release will be created"
            # Запускаем реальный релиз
            semantic-release version --noop  # Только версия и changelog, без push
            semantic-release changelog --unreleased-only
          else
            echo "🟢 No new release needed"
            exit 0
          fi

      - name: "📄 Commit and push changes"
        if: success()
        run: |
          # Проверяем, есть ли изменения в pyproject.toml или CHANGELOG.md
          if git diff --quiet pyproject.toml CHANGELOG.md; then
            echo "🟢 No changes detected"
          else
            echo "🔴 Changes detected - committing"
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add pyproject.toml CHANGELOG.md
            git commit -m "chore(release): auto-update version and changelog [skip ci]"
            git push
          fi
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: "🐍 Install Python"
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: "📦 Install dependencies"
        run: |
          python -m pip install --upgrade pip
          pip install ruff
      - name: "🧹Run Ruff"
        run: ruff check --output-format=github --config ./pyproject.toml .

  deploy:
    name: "🚀 Deploy to Server"
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: "🧾 Checkout code"
        uses: actions/checkout@v4

      - name: "🔐 Set up SSH"
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: "📦 Copy files to VPS"
        run: |
          rsync -avz --delete \
            -e "ssh -p 2233 -o StrictHostKeyChecking=no" \
            ./ ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:${{ secrets.DEPLOY_DIR }}/${{ github.event.repository.name }}

      - name: "📝 Generate .env file on server"
        run: |
          ssh -p 2233 -o StrictHostKeyChecking=no ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} << 'EOF'
          cat <<EOT > ${{ secrets.DEPLOY_DIR }}/${{ github.event.repository.name }}/.env
          POSTGRES_USER=${{ secrets.POSTGRES_USER }}
          POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }}
          POSTGRES_DB=${{ secrets.POSTGRES_DB }}
          POSTGRES_HOST=${{ secrets.POSTGRES_HOST }}
          POSTGRES_PORT=5432
          REDIS_HOST=${{ secrets.REDIS_HOST }}
          REDIS_PORT=6379
          REDIS_DB=0
          BOT_TOKEN=${{ secrets.BOT_TOKEN }}
          DEBUG=${{ secrets.DEBUG }}
          EOT
          EOF

      - name: "🚀 Start database for migrations"
        run: |
          ssh -p 2233 -o StrictHostKeyChecking=no ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} << 'EOF'
          cd ${{ secrets.DEPLOY_DIR }}/${{ github.event.repository.name }}
          docker compose down
          docker compose up -d --build database
          EOF

      - name: "🛠️ Run migrations"
        run: |
          ssh -p 2233 -o StrictHostKeyChecking=no ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} << 'EOF'
          cd ${{ secrets.DEPLOY_DIR }}/${{ github.event.repository.name }}
          echo "📁 Running Alembic migrations"
          docker compose run --rm --entrypoint="uv run alembic upgrade head" bot
          EOF

      - name: "🎯 Rebuild and start bot"
        run: |
          ssh -p 2233 -o StrictHostKeyChecking=no ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} << 'EOF'
          cd ${{ secrets.DEPLOY_DIR }}/${{ github.event.repository.name }}
          docker compose build --no-cache bot
          docker compose up -d bot
          EOF
